; (ns plan7.box.app.user
;   (:require
;     [plan7.box.vfs.core :as vfs])
;   (:gen-class))

; (defn >>
;   []
;   (keys (fs/find-in :home)))

; (defn >list
;   []
;   (keys (fs/find-in :home)))

; (defn >find
;   [name & {:keys [first-name]}]
;   (prn name)
;   (prn first-name))

; ; (defn >passwd
; ;   []
; ;   (prn :pws))