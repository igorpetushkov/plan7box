; (ns plan7.box.sh.bin.io.ask
;   (:gen-class))

; (defn ask-me
;   []
;   (prn :ask-me!))

; ; (defn >>
; ;   []
; ;   (read-line))

; ; (defn >wbf
; ;   [& [bf]]
; ;   (when bf
; ;     (print bf)
; ;     (flush))
; ;   (read-line))