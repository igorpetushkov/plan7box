; (ns plan7.box.vfs.loader
;   (:require
;     [plan7.box.sys.fs :as sys-fs])
;   (:gen-class))

; ; (defn load-sessions-from-file
; ;   [file-path]
; ;   (as-> (sys-fs/read-file file-path) f
; ;     (when f (read-string f))))

